components:
  schemas:
    Message:
      properties:
        id:
          format: int32
          readOnly: true
          type: integer
        obj:
          type: string
        predicate:
          type: string
        receiver:
          type: string
        sender:
          type: string
        subject:
          type: string
      required:
      - obj
      - predicate
      - receiver
      - sender
      - subject
      type: object
info:
  title: Shared DB Channel API
  version: 1.0.0
openapi: 3.0.2
paths:
  /messages:
    post:
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Message'
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Message'
